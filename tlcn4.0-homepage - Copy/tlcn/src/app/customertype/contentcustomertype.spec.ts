import { Contentcustomertype } from './contentcustomertype';

describe('Contentcustomertype', () => {
  it('should create an instance', () => {
    expect(new Contentcustomertype()).toBeTruthy();
  });
});
